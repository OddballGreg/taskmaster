<?php

function		maintain()
{
/*
	$index = -1;
	while ($GLOBALS['processes'][++index] != NULL)
	{
		//request status
		//if offline, request restart boolean setting
		//attempt to restart if true
		if (processes[index]->status() == FALSE)
		{
			*logFile << currentDateTime() << " " << processes[index]->name<< " " << processes[index]->pid << " Reported as OFFLINE\n";
			if (processes[index]->rstart_cond == TRUE)
			{
				*logFile << currentDateTime() << " " << processes[index]->name<< " " << processes[index]->pid << " Attempting To Restart Process\n";
				processes[index]->start();
			}
		}
		//request restartMe info
		//if true, restart the program(s)
		if (processes[index]->restartMe == TRUE)
		{
			*logFile << currentDateTime() << " " << processes[index]->name<< " " << processes[index]->pid << " Process detected as flagged for Restarting\n";
			*logFile << currentDateTime() << " " << processes[index]->name<< " " << processes[index]->pid << " Attempting To Restart Process\n";
			processes[index]->restartMe == FALSE);
			processes[index]->restart();
		}
	}	*/
}

function		autostart()
{
	/*int		index;

	index = -1;
	while (processes[++index] != NULL)
		if (processes[index]->autostart == TRUE)
			processes[index]->start();
			*/
}

?>